# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
# package-lock.json MUST be committed — it makes `npm ci` reproducible and
# ensures the correct platform-native deps (e.g. @tailwindcss/oxide) install.
yarn.lock
pnpm-lock.yaml
lerna.json
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Env files
.env
.env.local

